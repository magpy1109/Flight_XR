using UnityEngine;

public class PersistentPassthrough : MonoBehaviour
{
    public static PersistentPassthrough Instance { get; private set; }

    private void Awake()
    {
        if (Instance != null && Instance != this)
        {
            Destroy(gameObject);
            return;
        }

        Instance = this;
        DontDestroyOnLoad(gameObject);
    }
}